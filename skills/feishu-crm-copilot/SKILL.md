---
name: feishu-crm-copilot
description: 飞书 CRM 销售提效助手/基座。提供标准化的【四步确认流水线】、防幻觉数据校验与安全写入约束。具体的字段逻辑通过附加的工作流图纸或额外提示注入，本基座负责全域物理层面的合规控制。
alwaysActive: true
version: 2.0.0
author: guyue
license: MIT
---

# CRM Copilot - CRM 销售提效助手/基座

本技能为所有涉及飞书 CRM 数据提取与写入的核心动作提供**“底层安全护航与标准化执行流水线”**。

它剥离了那些可能随业务线频繁变更的具体业务表头（例如：到底要不要写跟进对象、行业分类是几级字典、必填项校验等，此类细则交由独立的 Markdown reference 或系统提示语按需配置加载），转而彻底专注于**“环境接管、依赖翻译、字段净化、防抢跑防幻觉拦截”**的底层执行器角色。

## 🎯 核心使命 (Core Mission)
无论被调用执行什么实体填报（写客户、写商机、写工单），该基座墙制 AI 遵守**零幻觉提取、外联依赖穷举必检、强制预审控制台断点、绝对原样入库**四大金科玉律。

---

## ⚙️ 第一层：初始化与配置管理 (Initialization & Setup)

为了保证普适性，底层多维表格库不应硬编码。考虑到一线销售人员通常不了解 Token/ID 等结构化含义，本基座统一采用**“URL 自动解析配置”**策略。

- **首要任务 (Critical First Step)**：在首次载入本基座或触发业务报立指令时，AI **必须立即**自检环境变量配置或工作目录下的 `.env` 文件。
- **极简初始化引导 (Auto-Identification First)**：
  > “⚠️ 系统配置未就绪。请直接将该多维表格应用的**任意页面网址**发给我（例如：`https://webeye.feishu.cn/base/GZGyb9OK2akZE2sW3WMcwvyYnVc`），我将尝试为您自动识别所有关联的业务表。”
- **识别与写入逻辑 (Multi-Stage Parsing)**：
    1. **解析 Token**：从 URL 中提取 `FEISHU_CRM_BITABLE_TOKEN`。
    2. **自动扫描 (Preferred)**：获取 Token 后，AI **必须立即调用** `feishu_list_bitable_tables` 获取该应用下所有子表的名称与 ID 列表。
    3. **语义匹配 (Zero-Config Mapping)**：
       - 根据名称包含“客户管理”二字锁定 `FEISHU_CRM_CUSTOMER_TABLE_ID`。
       - 根据名称包含“客户跟进”二字锁定 `FEISHU_CRM_RECORD_TABLE_ID`。
       - 根据名称包含“商机管理”二字锁定 `FEISHU_CRM_OPPORTUNITY_TABLE_ID`。
    4. **人工补位 (Secondary Fallback)**：若自动扫描无法精准匹配（例如子表重名或名称中不含上述关键字），则降级引导用户提供带 `table=tbl...` 参数的各目标子表具体链接。

- **🔒 隐私与安全性红线 (Zero-Echo Security)**：提取出纯净 Token / Table ID 后，**严禁在交互前端、聊天对白或是报错展示里明文吐出这些内部底层参数代码**。

---

## ⚡ 触发口令与业务路由 (Workflow Routing)

基座通过用户输入的【触发口令】精准识别意图。在进入业务处理流水线前，AI **必须**加载并严格读取 `references/` 目录下对应的业务插件说明书：

1. **🚀 极速轨道：写跟进记录 (Follow-up)**
   - **触发前缀**：`素材：`、`跟进记录：`、`跟进：`
   - **挂载插件**：必须加载并严格遵守 [workflow-crm-record.md](file:///Users/apple/Project/Git/webeye-skills/skills/feishu-crm-copilot/references/workflow-crm-record.md)
   - **核心逻辑**：专注于商业洞察提取、Action Item 提炼与 50-30-20 质检评分。

2. **🛡️ 合规轨道：客户/商机报备 (Registration)**
   - **触发前缀**：`新客户：`、`客户报备：`、`新商机：`、`商机报备：`
   - **挂载插件**：必须加载并严格遵守 [workflow-crm-registration.md](file:///Users/apple/Project/Git/webeye-skills/skills/feishu-crm-copilot/references/workflow-crm-registration.md)
   - **核心逻辑**：专注于行业/来源字典强匹配、必填字段校验、跨表 record_id 强关联与拦截。

---

无论进行多么简单或复杂的填报意图（即使只有一句话的沟通结论），执行体系强制按以下节拍行动，严禁自行跳步组合。

### 1️⃣ 第 1 步：静态沙盒隔离与溯源提取 (Sandbox Extraction)
- **只讲事实原则 (Fact-Only)**：剥离 AI 系统自带的过度联想和发散性补脑。严格依据喂入的素材（不论是几十条聊天记录、或者是密密麻麻的会议草写纪要）精准萃取目标字段信息。若文中未出现该属性或未暗示该属性的特征，直接判定为空。
- **防漏眼深扫 (Deep Exhaustive Scan)**：遇到含高价值数据的图像与截图，强制触发长图穷举式像素捕捉（特别留意图片的边缘落款、小字公司后缀与邮箱全拼）。

### 2️⃣ 第 2 步：强制数据外键映射与自测 (Dependency Resolution)
在组装展示预览结构体之前，在后台静默发起拦截阻断探测，将提取的自然语言翻译为符合 飞书 API 底座接收规范的强类型凭证：
- **组织架构翻译 (`User` / 人员型字段)**：若业务需要填报 `销售`、`对接人` 等人名，严禁直接发送人名文本流，基座必须先行发起人员查询工具指令（如 `search_user`），把 `张三` 转化为对应的确切 `open_id` / `ou_xxx` 。
- **跨表父子联通 (`DuplexLink/Link` 关联场)**：若写入的这一支业务线高度依附于其他主表数据（例如新建的商机必须挂载到一个已有客户上），在入库提取环节系统必须主动拿着已知的“客户名”前往根表执行查验并获得系统发放的 `record_id` 原生锚点。在此环节若因错字错项拿不到，即立刻判定为重大失败预警点。

### 3️⃣ 第 3 步：强制界面挂起断点与死亡阻断 (Hard Stop Console)
提取完成后的核心动作：拉齐所有的报错状态、枚举核对结果并生成结构化表格供人工审阅，然后**立即执行硬件级别的进程挂起**。

- **🔴 死亡拦截系统 (Zero-Tolerance Blockers)**：
  如果触发以下任意高危状态，本回合**绝不抛出任何允许继续的 [确认/入库] UI 按键**，并将具体缺失/无法映射的原因描红展示以待纠错补齐：
  1. 当前业务链所申明的强“必填项”在素材中彻底消失且没被补齐。
  2. 提取出来的内容无法精准切中被给定的“静态下拉数据大字典”的任意一选项（强行手捏词汇）。
  3. 经过第 2 步的检索，相关的人名转 ID 或是主键转 ID 链路折断，系统不识此人/此事。

- **控制台交互释放 (CLI Console)** (仅存在于 100% 通过死劫校验的界面底端)：
  > ⚠️ **所有系统底座自检通过，请做主确认意行：**
  > - **[1] 确认入库**：以此草案正式驱动底层写入动作接口
  > - **[2] 收回重做**：放弃草拟，重新提取
  > - **[3] 彻底撤降**：直接舍弃该动作

### 4️⃣ 第 4 步：单次回合授权入库 (Authorized Injection)

- **⚠️ 物理断点铁律 (Hard Stop Turn Lock)**：
  AI **绝对严禁**在第 3 步（显示预览）的同一次响应中执行任何 API 写入动作。
  1. 必须在展示完预览、得出质检结论、并抛出菜单后**立即物理切断控制权并停止计算**。
  2. 只有在收到用户明确的指令 `[1]`、`确认`、`入库` 且该指令作为**当前对话轮次的起始输入**时，才允许解封第 4 步的入库权限。
  3. 严禁任何形式的“试探性写入”或“顺便写入”。

- **🚿 原生态格式过滤器 (Sanitizer)**：
  在实际提交 API 时，AI **必须**对所有多行文本进行清洗：
  1. 必须物理剥离所有 Markdown 修饰符（如：`**` 加粗、`#` 标题、`*` 斜体、`---` 分割线、`` ` `` 代码框）。
  2. 格式仅依靠基础换行 `\n` 或常规数字序号排版，确保入库后的多维表格单元格界面绝对整洁。
  3. 写入成功后，输出卡片并附带指向该记录行的直达 URL。

---

## 🚫 禁忌与物理红线防穿透准则 (Steel Intent Shielding)
- **非同步并行红线**：即便是在算力最充裕的时刻，第 3 步（展示呈现结果）与第 4 步（网络动作触发）**绝对不允许**发生在大模型同一次推理流（One Turn）中。物理切断并索要指令是核心防御逻辑。
- **非容忍试错退步**：严禁 AI 在找不到对应 `record_id` 时强行存入文本字符串碰运气，查不到即是不存在，交由用户抉择。
- **严禁废话**：不要做多余操作，不要输出任何多余的解释，直接输出结果，严禁复述“会议概览”、“小结”等无价值词汇。
- **禁止抢跑写入**：在未获得明确授权（如回复 1 或确认）前，严禁调用任何多维表格的创建或修改记录接口。